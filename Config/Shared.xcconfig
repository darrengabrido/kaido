IPHONEOS_DEPLOYMENT_TARGET = 26.0
SWIFT_VERSION = 5.0

// Local defaults — Secrets.xcconfig may override these (TestFlight CI does).
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY =
PROVISIONING_PROFILE_SPECIFIER =

#include "Secrets.xcconfig"
